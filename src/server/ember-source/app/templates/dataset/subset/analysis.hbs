{{#each model.subset.usedBy as |method|}}
  {{#if method.analysisAppropriate}}
    {{ analysis-method method=method dataset=model.dataset createSubset=(route-action "createSubset") }}
  {{/if}}
{{/each}}

{{ method-selection dataset=model.dataset action=(route-action 'startAnalysis') }}

{{outlet}}